# ── Dependencias y build ──────────────────────
/node_modules
/.next/
/out/
/build
*.tsbuildinfo
next-env.d.ts

# ── Entorno ───────────────────────────────────
.env*

# ── Logs ──────────────────────────────────────
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ── Docs locales ──────────────────────────────
/docs

# ── Playwright CLI (su salida puede traer credenciales) ──
.playwright-cli/
.playwright/

# ── Skills instaladas via npx (reinstalables desde skills-lock.json) ──
.claude/skills/
.claude/agents/
.claude/settings.local.json

# ── Impeccable: logs transitorios de las paginas de decision ──
.impeccable/questions/

# ── Artefactos de build de la imagen OG (se regeneran) ──
design/og/og.build.html
design/og/font.b64

# ── Sistema ───────────────────────────────────
.DS_Store
Thumbs.db
*.pem
